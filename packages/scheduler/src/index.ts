import { add, after, before, build, createTag, getRunnable, getTag, hasTag, id, remove, removeTag, run, tag, create, debug } from './scheduler';

export * from './scheduler';

export default {
    add,
    before,
    after,
    id,
    tag,
    create,
    run,
    removeTag,
    hasTag,
    createTag,
    build,
    remove,
    getRunnable,
    getTag,
    debug
}